{% extends 'base.html.twig' %}

{% block body %}
        <div class="row">

            <h1>Section creation</h1>

            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input class="btn btn-success" type="submit" value="Créer" />
            {{ form_end(form) }}

<br>
            <a class="btn btn-primary" href="{{ path('admin_section_index') }}">Retour à la liste des sections</a>


        </div>

{% endblock %}
